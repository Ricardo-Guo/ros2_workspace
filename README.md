# ROS2 Robotic Arm Simulation  
## Gazebo + MoveIt2 + ros2_control + Pick & Place Tasks

---

## 📌 Project Introduction

This project implements a complete **ROS2 robotic arm simulation framework** based on a simplified **Franka Panda manipulator**.

It provides an **industrial-style full stack robotics pipeline**:

Robot Model → Controller → Physics → Motion Planning → Task Execution

The system integrates:

- URDF/Xacro robot modeling
- ros2_control controllers
- Gazebo physics simulation
- MoveIt2 motion planning
- RViz visualization
- High-level task execution (Pick & Place)

---

## 🧠 System Architecture

```
Task Layer (pick_place / gripper / lift)
            ↓
MoveIt2 (planning)
            ↓
ros2_control (controllers)
            ↓
Gazebo (physics simulation)
            ↓
URDF Robot Model
```

---

## 📂 Workspace Structure

```
ros2_workspace/
└── src/
    ├── your_robot_description   # Robot model + RViz
    ├── your_robot_gazebo       # Gazebo + ros2_control backend
    ├── your_robot_moveit       # MoveIt2 planning config
    └── your_robot_tasks        # Task-level behaviors (Pick & Place)
```

Each package has a **single responsibility**, following industrial ROS design principles.

---

# 📦 Package Details

---

# 1️⃣ your_robot_description
## Robot Model + RViz Visualization

Contains only the **pure robot description**.

### Structure

```
your_robot_description
├── config
│   └── controllers.yaml        # RViz joint control parameters
├── launch
│   ├── display.launch.py       # Full RViz visualization
│   └── rsp.launch.py           # robot_state_publisher only
├── meshes
│   ├── collision               # collision meshes
│   └── visual                  # visual meshes
├── urdf
│   ├── simplified.urdf
│   └── simplified.xacro
├── CMakeLists.txt
└── package.xml
```

### Run

```bash
ros2 launch your_robot_description display.launch.py
```

---

# 2️⃣ your_robot_gazebo
## Gazebo + ros2_control Simulation Backend

Responsible for:

- Physics simulation
- Controller management
- Hardware abstraction
- MoveIt execution bridge

---

### Structure

```
your_robot_gazebo
├── config
│   ├── controllers.yaml
│   └── moveit_controllers.yaml
├── launch
│   ├── gazebo_launch.py
│   └── gazebo_moveit.launch.py
├── meshes
├── urdf
│   ├── simplified.urdf
│   ├── moveit.urdf
│   └── gazebo_moveit.urdf
├── CMakeLists.txt
└── package.xml
```

---

## 🔹 Gazebo Only Mode (Joint Control)

Start Gazebo:

```bash
ros2 launch your_robot_gazebo gazebo_launch.py
```

Load controllers:

```bash
ros2 run controller_manager spawner joint_state_broadcaster
ros2 run controller_manager spawner arm_position_controller
```

Send joint command:

```bash
ros2 topic pub /arm_position_controller/commands \
std_msgs/msg/Float64MultiArray \
"{data: [0.0, -0.5, 0.0, -1.0, 0.0, 1.0, 0.0]}"
```

---

## 🔹 Gazebo + MoveIt Mode (Recommended ⭐)

```bash
ros2 launch your_robot_gazebo gazebo_moveit.launch.py
```

Then load:

```bash
ros2 run controller_manager spawner joint_state_broadcaster
ros2 run controller_manager spawner arm_controller
```

Use **RViz MoveIt GUI** to plan and execute trajectories.

---

# 3️⃣ your_robot_moveit
## MoveIt2 Planning Configuration Package

Generated by MoveIt Setup Assistant.

Provides:

- Kinematics
- Planning groups
- Controller mapping
- Motion planning

### Structure

```
your_robot_moveit
├── config
├── launch
├── meshes
├── urdf
├── CMakeLists.txt
└── package.xml
```

### Run

```bash
ros2 launch your_robot_moveit demo.launch.py
```

---

# 4️⃣ your_robot_tasks ⭐
## Task-Level Manipulation Behaviors

Implements high-level automation on top of MoveIt.

This layer performs **complete pick & place actions**.

---

### Structure

```
your_robot_tasks
├── config
├── models
├── resource
├── test
└── your_robot_tasks
    ├── pick_place_node.py
    ├── gripper_close_node.py
    └── lift_object_node.py
```

---

## Nodes

### pick_place_node
Move TCP to object grasp pose.

```bash
ros2 run your_robot_tasks pick_place_node
```

---

### gripper_close_node
Close the gripper to grasp the object.

```bash
ros2 run your_robot_tasks gripper_close_node
```

---

### lift_object_node
Lift the grasped object to target position.

```bash
ros2 run your_robot_tasks lift_object_node
```

---

# 🚀 Build Instructions

```bash
cd ~/ros2_workspace
colcon build
source install/setup.bash
```

---

# 🧪 Typical Workflows

---

## Visualization Only

```bash
ros2 launch your_robot_description display.launch.py
```

---

## Gazebo Simulation Only

```bash
ros2 launch your_robot_gazebo gazebo_launch.py
```

---

## Gazebo + MoveIt Planning

```bash
ros2 launch your_robot_gazebo gazebo_moveit.launch.py
```

---

## Full Pick & Place Pipeline ⭐

```bash
ros2 launch your_robot_gazebo gazebo_moveit.launch.py
ros2 run your_robot_tasks pick_place_node
ros2 run your_robot_tasks gripper_close_node
ros2 run your_robot_tasks lift_object_node
```

---

# 🛠 Tech Stack

- ROS2 Humble
- Gazebo
- MoveIt2
- ros2_control
- URDF/Xacro
- RViz2
- Python

---

# ⭐ Highlights

- Modular architecture
- Clean package separation
- Gazebo + MoveIt integration
- ros2_control hardware abstraction
- Task-level automation
- Easy extension to real hardware
- Industrial-style design

---

# 👤 Author

Ricardo Guo

---

# 📄 License

MIT
